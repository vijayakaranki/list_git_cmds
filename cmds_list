*****jenkins installation commands*****
1.wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo
2.rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.repo
3.yum install jenkins
4.sudo service jenkins start
# jenkins admin password location: /var/lib/jenkins/secrets/initialAdminPassword
******Git Commands******
1.yum install git
2.git --version
#setting up username and email for git
 git config --global user.name "name comes here"
 git config --global user.email "email id comes here"
#central repository cloning
 git clone <github repository url>
#creating,editing,commit and pushing files to central repo
$cd <repo name>
$touch file1 file2
$cat >file1 ----->adding stuff to file1
$git status----->checking file status
$git add -A/<file name>/. ------->updating files
$git branch ----->checking in which branch we are currently in
$git checkout <branch/master> ------>shifting to master or branch
$git commit -m "changes done"---->commit the changes
$git push -u origin <master/branch> ------>pushing files to repo
